{
  "routes": [
    {
      "method": "PUT",
      "path": "/users/me",
      "handler": "User.updateMe",
      "config": {
        "policies": [],
        "prefix": "",
        "description": "Update the authenticated user only",
        "tag": {
          "plugin": "users-permissions",
          "name": "User",
          "actionType": "update"
        }
      }
    }
  ]
}
